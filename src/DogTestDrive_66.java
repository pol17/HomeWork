public class DogTestDrive_66 {

    //главный метод
    public static void main(String[] args) {

        //создаем объект класса Dog_66
        Dog_66 d = new Dog_66();

        //устанавливаем значение поля size через оператор доступа (точка)
        d.size = 40;

        //вызываем метод bark() через оператор доступа (точка)
        d.bark();
    }
}
